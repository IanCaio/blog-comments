post1comments:
  allowedFields: ["parent", "github", "name", "comment"]
  branch: "master"
  filename: "{@timestamp}-{@id}"
  format: "json"
  moderation: false
  path: "_data/post1comments"
  requiredFields: ["name", "comment"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"

post2comments:
  allowedFields: ["parent", "github", "name", "comment"]
  branch: "master"
  filename: "{@timestamp}-{@id}"
  format: "json"
  moderation: false
  path: "_data/post2comments"
  requiredFields: ["name", "comment"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"

post3comments:
  allowedFields: ["parent", "github", "name", "comment"]
  branch: "master"
  filename: "{@timestamp}-{@id}"
  format: "json"
  moderation: false
  path: "_data/post3comments"
  requiredFields: ["name", "comment"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"

post4comments:
  allowedFields: ["parent", "github", "name", "comment"]
  branch: "master"
  filename: "{@timestamp}-{@id}"
  format: "json"
  moderation: false
  path: "_data/post4comments"
  requiredFields: ["name", "comment"]
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
